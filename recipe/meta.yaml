{% set name = "tiptop" %}
{% set version = "0.0.17" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/tiptop-{{ version }}.tar.gz
  sha256: 7bf1ed695c63a33b7ceb6fd9a0326a8cad91bafbba749fd1a3389aeabbb400d5

build:
  noarch: python
  number: 0
  script: {{ PYTHON }} -m pip install . -vv
  entry_points:
    - tiptop = tiptop._app:run

requirements:
  host:
    - pip
    - python >=3.7
  run:
    - distro
    - importlib-metadata
    - psutil
    - py-cpuinfo
    - python >=3.7
    - rich
    - textual

test:
  imports:
    - tiptop
  commands:
    - pip check
    - tiptop -h
  requires:
    - pip

about:
  home: https://github.com/nschloe/tiptop
  summary: A command-line system monitoring tool in the spirit of top.
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - thewchan
